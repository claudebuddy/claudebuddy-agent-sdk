# GitHub Packages registry mapping + token placeholder.
# Copy this file to `.npmrc` and export GITHUB_TOKEN in your environment.
# NEVER commit a real token — keep using the ${GITHUB_TOKEN} placeholder.
@claudebuddy:registry=https://npm.pkg.github.com/
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}